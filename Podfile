platform :ios, '9.0'
use_frameworks!

target 'MeuApt' do
  pod 'Alamofire', '~> 4.5'
  pod 'Marshal', '~> 1.2'
  pod 'Kingfisher', '~> 3.13'

  target "MeuAptTests" do
    inherit! :search_paths
  end
end
